using Microsoft.SPOT;

namespace extra {
	using properties;

	public static class Fonts {
		public static Font small = Resources.GetFont(Resources.FontResources.small);
		public static Font ninaB = Resources.GetFont(Resources.FontResources.NinaB);
	}
}